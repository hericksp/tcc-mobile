<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    tools:context=".ui.MainActivity">


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:orientation="vertical">

        <Button
            android:id="@+id/acesso_sindico"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_below="@+id/header_image"
            android:layout_marginHorizontal="32dp"
            android:layout_marginTop="32dp"
            android:backgroundTint="@color/primary"
            android:drawableStart="@drawable/ic_baseline_person_24"
            android:text="ACESSO SÍNDICO"
            android:textColor="@color/white"
            app:cornerRadius="18dp"
            app:icon="@drawable/ic_baseline_person_24"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/acesso_morador" />

        <ImageView
            android:id="@+id/header_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="120dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/towersadmin" />

        <Button
            android:id="@+id/acesso_morador"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_below="@+id/header_image"
            android:layout_marginHorizontal="32dp"
            android:layout_marginTop="128dp"
            android:backgroundTint="@color/primary"
            android:drawableStart="@drawable/ic_baseline_house_24"
            android:text="ACESSO MORADOR"
            android:textColor="@color/white"
            app:cornerRadius="18dp"
            app:icon="@drawable/ic_baseline_person_24"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/header_image" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>

